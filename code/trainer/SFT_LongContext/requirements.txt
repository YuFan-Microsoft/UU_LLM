accelerate
datasets
deepspeed==0.18.9
einops
# flash-attn and ring_flash_attn must be installed AFTER torch with build
# isolation disabled. See install.sh.
# Install with:  pip install flash-attn==2.8.3 --no-build-isolation
# flash-attn==2.8.3
huggingface_hub>=1.0.0
jsonlines
packaging
peft
torch
torchmetrics
tqdm
transformers==5.5.4
wandb
wheel